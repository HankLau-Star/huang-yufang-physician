# AGENTS.md

本文件是提供给后续 AI 编程助手的项目级工作说明。修改任何文件前，请先完整阅读本文件和 `README.md`。

## 项目目标

维护黄玉芳医师的公开个人网站。网站应呈现可信、庄重、有温度的医生形象，并保留现代西医临床与中医文化相融合的视觉语言。核心受众使用中国大陆手机和微信内置浏览器。

## 不可破坏的要求

- 默认语言为简体中文，英文只作为克制的辅助层级。
- 必须优先保证 360px–430px 手机屏幕和触控操作。
- 不得引入依赖境外 CDN 才能显示的字体、脚本或核心图片。
- 使用仓库内现有字体和素材；不要擅自替换医师照片或改变人物面貌。
- 保留电话 `15038264053`、微信号 `a15038264053` 及一键复制功能，除非所有者明确要求修改。
- 保留医疗免责声明、急危重症提示和“具体诊疗须当面评估”的边界表达。
- 不承诺疗效，不增加未经确认的职称、荣誉、机构关系或诊疗资质。
- `.openai/hosting.json` 已绑定现有主站，禁止删除、重建或自行更换 `project_id`。
- 不要提交 `.env`、访问令牌、登录信息、临时压缩包、构建缓存或患者资料。

## 设计约束

- 主色：深墨蓝绿、玉色、纸张暖白、克制金色与印章红。
- 气质：医学档案、医院建筑秩序、宋体书卷感、适量篆刻元素。
- 避免：高饱和霓虹、通用模板感、厚重玻璃拟态、过度圆角、夸张弹跳动效、大面积纯白按钮。
- 动效需服务信息层级，并兼容 `prefers-reduced-motion`。
- 所有新增图片必须设置准确 `alt`、固有尺寸，并优先懒加载非首屏图片。

## 主要修改入口

- 内容和组件：`app/page.tsx`
- 视觉和响应式：`app/globals.css`
- 页面元信息：`app/layout.tsx`
- 图片素材：`public/`
- GitHub Pages 导出：`scripts/export-github-pages.mjs`
- 自动检查：`tests/rendered-html.test.mjs`

尽量延续现有单页结构和 CSS 变量，避免为了小改动引入新的 UI 框架、数据库、登录系统或第三方追踪服务。

## 完成标准

每次修改完成后至少执行：

```bash
npm run build
node --test tests/rendered-html.test.mjs
```

若准备同步 GitHub Pages，再执行：

```bash
npm run export:github
```

确认 `docs/` 已包含本次新增的图片与最新静态页面，然后提交源码和 `docs/`。不要只提交编译产物，也不要只提交源码而忘记更新 `docs/`。

## 部署策略

- GitHub Pages：公开、可迁移的长期备份与独立入口；从 `main/docs` 发布。
- OpenAI Sites：当前主站；仅在拥有对应 Sites 工具与项目权限时复用现有项目发布。

如果无法访问其中一个平台，应完成可验证的源码修改并明确说明尚未同步的目标，不要创建同名新站点代替原项目。

